<?php
/**
 * Rendered before dispatch when MAINTENANCE_MODE is on, so nothing here may depend on the
 * application having loaded.
 *
 * No search and no internal links: every path answers with this same page while the flag is set,
 * so offering them would send a reader in a circle. The links out still work.
 *
 * Expects: $title   Optional: $code
 */
$code    = $code ?? 503;
$heading = 'Down for a short while';
$body    = 'The documentation is being updated and will be back shortly. The README in each '
         . 'component repository covers the same ground in the meantime.';

$internalLinks = false;

$actions = [
    ['href' => 'https://github.com/popphp', 'label' => 'Pop on GitHub'],
    ['href' => 'https://www.popphp.org',    'label' => 'popphp.org'],
];

include __DIR__ . '/partials/status.phtml';
